# HM3D Data Setup

Keep each `.basis.glb` beside its matching `.basis.navmesh`,
`.semantic.glb`, and `.semantic.txt`. Keep the annotated scene dataset config
at the directory level expected by its relative glob paths. The inspection CLI
checks navmesh area, semantic objects, and one synchronized sensor frame.

